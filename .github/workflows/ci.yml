name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Typecheck + Build
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: TypeScript check
        run: npm run typecheck

      - name: Build (Next.js — static export)
        run: npm run build
        env:
          # Valores só para o build compilar no CI. Em produção as
          # NEXT_PUBLIC_* entram no ambiente do build de deploy (ver DEPLOY.md).
          NEXT_PUBLIC_SITE_URL: "https://julianamaianutriaplv.com.br"
          NEXT_PUBLIC_WHATSAPP_NUMBER: "5531999191083"
          NEXT_PUBLIC_WHATSAPP_SUPPORT: "5531999191083"
          LEAD_PROVIDER: "none"
